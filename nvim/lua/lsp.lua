lspconfig = require('lspconfig')
lspconfig.gopls.setup{}

-- lspconfig.solargraph.setup{
--   settings = {
--     solargraph = {
--       definitions = true,
--       references = true,
--       completion = true
--     }
--   }
-- }
